apiVersion: v1
kind: Service
metadata:
  name: element-web
  namespace: matrix-system
  labels:
    app.kubernetes.io/name: element-web
    app.kubernetes.io/part-of: matrix
spec:
  selector:
    app.kubernetes.io/name: element-web
  ports:
    - name: http
      port: 80
      targetPort: 80
      protocol: TCP
